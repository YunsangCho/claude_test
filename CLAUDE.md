# 한국사 카드덱 — 작업 지침

와이프의 한국사능력검정시험 대비 학습 앱이다. 단일 HTML 파일 하나로 폰에서 열어 쓴다.
외부 라이브러리를 쓰지 않고 인터넷 없이 동작해야 한다.

## 명령

```bash
python scripts/build.py     # 데이터 + 앱 → dist/한국사-카드덱.html
node   scripts/check.mjs    # 중복, 해설 누락, 보기 품질 검증
```

문항을 고친 뒤에는 반드시 build 후 check 를 돌린다. check 가 실패하면 고치고 다시 돌린다.

## 구조

```
src/index.template.html   화면 뼈대와 CSS.  <!--BUNDLE--> 자리에 스크립트가 들어간다
src/app.js                앱 로직, 문항 생성 엔진, 간격 반복, 모의고사
data/cards/               강별 문답 카드
data/questions/           OX, 빈칸, 사료, 비교표, 순서 배열
data/structured/          주체, 사건, 문화유산, 도식.  엔진이 문제를 만들어 내는 원본
scripts/manifest.txt      빌드에 넣을 파일 순서.  파일을 새로 만들면 여기에 추가한다
docs/images.csv           사진 목록. 이름, 강, 검색어, 묶음, 이어 줄 항목, 출처, 라이선스
```

## 데이터 형식

카드 `[강, 질문, 정답, 해설]`
- 해설은 반드시 넣는다. 사실만 되풀이하지 말고 왜 그랬는지, 무엇과 헷갈리는지를 쓴다.
- 정답이 `"A / B"` 형태로 둘을 묶으면 객관식에서 제외되니 꼭 필요할 때만 쓴다.

OX `[강, 진술, 1 또는 0, 해설]` — 틀린 진술은 실제로 헷갈리는 함정으로 만든다.
빈칸 `[강, "___ 가 들어간 문장", 정답, 해설]`
사료 `[강, 제시문, 질문, 정답, [오답1, 오답2, 오답3], 해설]` — 제시문은 원문을 그대로 옮기지 말고 요지를 풀어 쓴다.
비교표 `[강, 제목, A, B, [[항목, A값, B값], ...]]` — 행 하나마다 문제 두 개가 생긴다.
순서 `[강, 제목, [1단계, 2단계, ...]]`

주체 `[강, 이름, 계열, 정렬연도, [설명들], [관련유물], 첫힌트]`
- 인물뿐 아니라 나라, 시대, 제도도 넣는다.
- 설명은 4개 이상. 여기서 틀린 것 찾기, ㄱㄴㄷㄹ 조합, 정체 추론이 나온다.
- 계열이 같은 항목끼리 오답 보기를 주고받으므로 계열을 정확히 맞춘다.

사건 `[강, 사건명, 연도, 시대키]` — 기원전은 음수. 연표 시기 판별에 쓴다.
문화유산 `[강, 이름, 계열, 갈래, [특징들]]`
도식 `[강, 이름, 묶음, 질문, SVG, 해설]` — 색은 반드시 `var(--ink)` 같은 CSS 변수로 쓴다.

## 지켜야 할 것

- 문항 중복 금지. 다른 강에 같은 질문을 넣지 않는다.
- 객관식 오답은 같은 계열, 같은 시대에서 뽑는다. 엔진이 처리하므로 데이터의 계열만 정확하면 된다.
- 사실 관계는 교과서 수준으로 확인한다. 연도와 인물을 헷갈리게 두지 않는다.
- 한 강의 문항이 60개 미만이면 check 가 경고한다. 1강은 도입 단원이라 예외로 둔다.
  23강과 32강은 구조화 데이터를 채우면서 경고에서 벗어났다.

## 남은 작업

1. 구조화 데이터는 1~40강 전 범위를 채웠다.
   주체 610, 사건 574, 문화유산 195다. 주체와 사건은 목표(300, 600)에 닿았다.
   문화유산은 목표 300에 못 미치는데, 근현대는 유물보다 사진으로 다루는 편이 맞아
   나머지는 사진 작업으로 메운다.
2. 사진을 더 모은다. `docs/images.csv` 에 243행이 올라 있고 그만큼을 채우는 것이 목표다.
   한능검에서 사진으로 자주 출제되는 것을 기준으로 골랐다.
3. 도식은 지도와 조직도, 고분 구조도만 남긴다.
   석탑 도식 8개는 사진이 들어오면서 지웠다. 실루엣으로 층수만 구분하던 것이라 쓸 자리가 없다.
   남은 15개는 사진으로 대체할 수 없다. 전성기 지도는 영토 범위를 그린 것이고
   조직도는 관청 계통도라 실물이 없다. 고분 구조도도 무덤 단면이라 장군총 사진으로는
   돌무지무덤과 굴식 돌방무덤의 차이를 볼 수 없다. 이 다섯 묶음은 한능검 사진 문제의 단골이다.

## 사진 넣는 절차

배선은 이미 끝났다. 사진을 늘릴 때는 1번과 2번만 하면 된다.

1. `docs/images.csv` 에 행을 더한다. 채워야 할 칸은 이렇다.
   - `search` 와 `search_alt` — 한글 검색어와 영문 검색어. 한글만으로는 엉뚱한 것이 잡히는 경우가 있다.
     `주먹도끼` 로 검색하면 프랑스와 아프리카의 주먹도끼가 나온다. `전곡리 주먹도끼` 로 바꿔야 한다.
   - `group` — 그림 문제의 오답을 같은 묶음에서 뽑는다. 묶음마다 4개 이상이어야 한다.
     시대가 뒤섞이면 문제가 쉬워진다. 건축은 고대, 고려, 조선, 근대로 나눠 두었다.
   - `match` — 구조화 데이터의 항목 이름. 글자까지 같아야 이어진다.
     이어지면 사진이 그 항목의 틀린 것 찾기와 ㄱㄴㄷㄹ 조합 문제에도 뜬다.
2. `python scripts/fetch_images.py` 를 돌린다. 위키미디어 커먼즈를 검색해
   `url`, `license`, `credit`, `page` 를 스스로 채우고 `images/raw/` 에 받는다.
   국가유산포털(heritage.go.kr)은 이 환경에서 연결이 끊겨 쓸 수 없다.
   공유 IP 라 커먼즈가 429 를 자주 던진다. 요청 사이에 6초를 두므로 243행을 도는 데 한 시간쯤 걸린다.
3. 받은 사진을 눈으로 확인한다. 모형이나 기념품, 엉뚱한 유물이 섞인다.
   제목에 `replica`, `모형`, `복제` 가 있으면 스크립트가 거르지만 그것만으로는 부족하다.
4. `pip install pillow` 후 `python scripts/optimize_images.py --width 600 --quality 72 --budget 8` 을 돌린다.
   `images.csv` 의 정보를 붙여 `data/structured/photos.js` 를 만든다.
   라이선스 칸이 빈 사진은 넣지 않는다.
5. build 와 check 를 돌린다.

`photos.js` 는 깃에 추적된다. `images/raw/` 가 gitignore 라 CI 가 사진을 다시 만들 수 없고,
완성본이 저장소에 있어야 배포가 되기 때문이다.
다시 만들 때마다 깃 히스토리에 통째로 새 덩어리가 쌓이므로 자주 돌리지 않는다.

### 용량

base64 로 담기면 원본의 약 4/3 이 된다. 117장 실측치는 이렇다.

| 설정 | JPEG | WebP |
|---|---|---|
| 600px 품질 72 | 8.81MB | 7.32MB |
| 520px 품질 68 | 6.16MB | 5.23MB |
| 480px 품질 66 | 5.05MB | 4.33MB |
| 400px 품질 62 | 3.32MB | — |

WebP 는 17% 정도만 줄어 바꿀 만하지 않다.
600px 품질 72 를 쓰기로 했다. 유물 세부를 알아보려면 이 아래로는 내리기 어렵다.
`optimize_images.py` 는 `--budget` 을 넘으면 폭과 품질을 스스로 낮춘다.

## 저작권

앱을 GitHub Pages 로 공개 배포한다. 개인 사용이 전제였던 때와 조건이 달라졌다.

사진은 위키미디어 커먼즈에서 받고 자유 라이선스만 쓴다.
`fetch_images.py` 가 CC0, Public Domain, CC BY, CC BY-SA, KOGL Type 1 만 통과시킨다.
KOGL Type 1 은 공공누리 제1유형이고 출처만 밝히면 변형과 상업적 이용까지 된다.
제2유형은 상업적 이용 금지, 제3유형은 변경 금지라서 사진을 줄여 쓰는 이 작업에는 맞지 않는다.

받은 사진의 60%가 CC BY 계열이라 저작자 표시가 필요하다.
`credit` 칸에 저작자와 라이선스를 적고 앱에서 사진 아래에 한 줄로 보여 준다.
라이선스 칸이 비어 있으면 `optimize_images.py` 가 그 사진을 넣지 않는다.
출처가 불분명한 사진은 쓰지 않는다는 원칙을 코드로 옮긴 것이다.
